# PickPlace1B

Progress page for a simulated tabletop pick-and-place dataset rendered in Isaac Sim, heading for one billion RGB frames.

Open `index.html` (served by GitHub Pages). Numbers are a snapshot counted from the saved episodes; `data.js` holds the aggregated statistics and `media/` the videos and lighting images.
